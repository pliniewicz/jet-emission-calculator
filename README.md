# Installation
Requires:
* GSL

```make```

That's it. Binary sits in the `bin` folder

# What does it do by far?
1. Calculates the magnetic field pressure $P_B$ normalization from a given profile under equipartition condidtion, for given jet parameters.
2. (almost) Normalizes the electron density distribution (still needs to get physical dimensions into that) 
